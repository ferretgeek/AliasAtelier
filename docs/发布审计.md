# 发布审计 / Release Audit

## 2026-08-09 preview refresh

- The generated-result screen is now the primary 1280×640 profile and social preview; the README also keeps the full workbench and entry composition.
- Every visible address and metadata field was generated from the built-in safe example and reviewed as synthetic before publication.
- The refreshed PNG files remain subject to size, metadata, OCR, and secret-scan gates.

## v1.0.0

状态：v1.0.0 已作为不可变 Release 发布，功能、视觉、隐私、在线安全与公开新克隆门禁全部通过。

### 私人来源隔离

- 原始目录中的私人 TXT 没有复制到本仓库。
- 旧脚本内针对私人 API 地址的特殊改写逻辑没有进入公开实现。
- 示例地址、附加字段和界面数据均从零创建，不来自原始账号文件。
- `.gitignore` 默认拦截 TXT、私有目录、数据目录和导出目录。

### 待完成门禁

- [x] Node 核心测试：8 项通过
- [x] Python 服务器与静态契约测试：7 项通过
- [x] Python 编译、Ruff、Bandit、pip-audit、detect-secrets
- [x] Gitleaks 工作树与全部完整可达提交
- [x] 桌面实景与四套主题；响应式断点由静态契约和 CSS 检查覆盖
- [x] 1280×720 界面图与 1280×640 社交预览图
- [x] PNG 元数据与画面隐私复核，仅含 DPI 信息和合成地址
- [x] GitHub Actions / JavaScript 与 Python CodeQL
- [x] Secret Scanning / Push Protection / 私密漏洞报告
- [x] 依赖图、Dependabot 警报、恶意软件告警、自动及分组安全更新
- [x] Dependabot 的 Nginx 1.31 与 setup-node 7 更新经 CI / CodeQL 通过后压缩合并
- [x] 1280×640 图片已上传 GitHub Social preview
- [x] v1.0.0 不可变 Release，标签指向经验证的 `5550c90`
- [x] 未登录公开克隆：8 + 7 项测试、pip-audit、detect-secrets、Gitleaks 当前文件与 5 个公开提交历史全部通过
- [x] 公开克隆只包含 35 个跟踪文件，提交邮箱仅为 GitHub `noreply` 身份
- [x] 仓库与 Release 未登录访问均返回 HTTP 200，公开 `og:image` 使用已上传的 GitHub 仓库社交预览

## English summary

The clean public project contains no source TXT data or private API-specific rewriting. All examples are synthetic. The immutable v1.0.0 release and a fresh unauthenticated clone have both passed the complete release gate.
