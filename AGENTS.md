# Alias Atelier 项目规则

- 先阅读根目录 `AGENTS.md` 与本项目 `README.md`。
- 产品定位是纯本地、零上传的邮箱标签地址编排工具；不得添加遥测、外部字体、远程脚本或需要传输用户输入的 API。
- 用户内容不得进入持久化存储、URL、日志、截图、测试夹具或示例。`localStorage` 只允许保存非敏感的主题名称。
- Gmail 与 Microsoft 可按公开文档说明；iCloud 和自定义域必须继续标为兼容性实验，不得宣称投递必然成功。
- 附加字段默认剥离，预览始终遮挡。改动不得削弱 CSP、固定文件路由、输入大小与输出数量上限。
- UI 必须同步维护四套全局主题，其中 Graphite 为深灰色暗色主题；桌面与移动端均需实际渲染检查。
- 公开改动必须同步双语 README、截图、社交预览、`docs/发布审计.md`、工作区根 `README.md` 与 GitHub 主页仓库。
